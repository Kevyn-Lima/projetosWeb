const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado'

console.log(resultado(7.1))
console.log(resultado(6.6))

// operador ternario, sempre representado em tres partes, a primeira é a ação a função se vem o ? e depois uma resposta se for verdadeira, dois pontos, e outra se for falsa.